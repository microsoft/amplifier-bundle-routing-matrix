"""SCRATCH — deliberately failing test. Do not merge.

Same purpose as its sibling under modules/hooks-routing/tests/: proves the
`Tests — root` CI job actually executes tests/. Deleted with its branch.
"""


def test_ci_red_proof_root_suite_runs():
    assert False, "deliberate failure: proving Tests — root runs"
