# Routing Matrix: openai
#
# OpenAI-only routing. All roles use OpenAI models exclusively.
# Use when you want to run entirely on OpenAI -- EITHER billing path.
#
# BOTH OPENAI BACKENDS, ONE MATRIX. Every role carries two candidates for the
# same model: `provider: openai` (the pay-per-use API-key backend) and
# `provider: openai-chatgpt` (the ChatGPT-subscription/OAuth backend). They
# serve the same gpt-5.6 family and resolve to the same model id; the only
# thing that differs is which account pays. Whichever you have mounted wins,
# and having both just means the API key is tried first.
#
# They need SEPARATE candidates because they are separate provider MODULES,
# not two instances of one: `find_provider_by_type` matches neither by name
# nor via its module-type fallback, so a lone `provider: openai` candidate
# leaves a ChatGPT-only user with no routing at all.
#
# And their `config:` blocks differ, unavoidably: provider-openai reads
# `reasoning_effort` from mount config, provider-openai-chatgpt does not (its
# only effort read is `request.reasoning_effort`, which nothing populates from
# config). The latter's live knob is `extra_request_params.reasoning.effort`,
# the exact payload block it builds for itself. The loader enforces this --
# a `reasoning_effort:` on a chatgpt candidate is rejected, not ignored.
#
# TIER STATUS (2026-09-07): the flagship `gpt-?.?-sol*` tier is PAUSED pending
# further evals. No role selects sol. Roles that previously did now use
# `gpt-?.?-terra*` one effort notch higher, clamped at xhigh.
#
# `luna` is used for exactly two roles: `fast` (cheap utility work) and
# `ui-coding`, the latter at `max` effort -- cheapest tier, maximum thinking.
#
# TWO GLOBS, NO FALLBACKS, NO TRAILING `*`. Every candidate in this file is
# either `gpt-?.?-terra` or `gpt-?.?-luna`. The `gpt-[0-9].[0-9]` base-alias
# twin (a pre-5.6 migration fallback) and the `gpt-?.?-mini*` rung are both
# gone -- gpt-5.6 terra and luna are live on both backends, so neither
# fallback bought anything.
#
# THE MISSING `*` IS LOAD-BEARING. The ChatGPT backend serves a `-fast`
# sibling for every model (`gpt-5.6-terra` AND `gpt-5.6-terra-fast`). A
# trailing `*` matches both, they tie on version, and the resolver's tie-break
# prefers the LONGER name -- so `gpt-?.?-terra*` silently selects `-fast`.
# Suffix-free, the SAME glob resolves to `gpt-5.6-terra` on both backends,
# which is why one glob vocabulary can serve both. (Same shape as the
# `gpt-5.6-sol-fast` "Internal only" trap on GitHub Copilot.) It also stops
# matching dated snapshots, which is wanted: the clean alias is the target.
#
# CONSEQUENCE, STATED PLAINLY: each role offers one candidate per backend and
# nothing else, and neither openai provider has a static model-list fallback
# (provider-openai's `list_models()` docstring: "no fallback; caller handles
# empty lists"). If that call fails, the glob resolves to nothing and the role
# yields no preference -- the agent falls back to the session's default
# provider rather than erroring. If that degradation is not acceptable, add a
# pinned exact name (e.g. `gpt-5.6-terra`) as a further candidate: an exact
# name never calls list_models(), the same glob+pin shape copilot.yaml uses.
#
# NOTE: `gpt-?.?-sol*` is deliberately RETAINED on the preset tier ladder
# below even though nothing routes to it. The ladder describes the provider
# family for knob-consistent tier comparison; removing the rung would
# misclassify a sol model that a user pins by hand. With the base alias gone
# from the roles, nothing resolves onto the sol rung any more, so a terra-tier
# caller can no longer be clamped by a fallback sitting a rung above it.
#
# Knob-consistent delegation is ON by default for this matrix (measured win on
# OpenAI roots: `gpt-5.6-sol` call share 27.8% -> 0.0%, cost -29.7% (S3 median)
# / -55.9% (S1), wall -16.2%, quality flat). Scope: OpenAI-family roots only --
# every other shipped matrix is untouched by the preset. Opt out with
# `disable_delegation_preset: true` in this hook's mount config to restore
# legacy (pre-2026-09-02) behaviour.
#
# NOTE (2026-09-07): `openai-knob-consistent.yaml` has been DELETED. It shipped
# as an opt-in arm carrying a byte-identical preset block; once the preset
# became this matrix's default on 2026-09-02 the two files were functionally
# the same file under two names. Select `openai` instead.
#
# --- Review log ---
# 2026-09-07: Curator refresh, verified against LIVE provider list_models()
#   output on 2026-09-07 (anthropic, openai, gemini, github-copilot).
#   1. OpenAI flagship (`gpt-?.?-sol*`) PAUSED pending further evals -- every
#      sol candidate moved down one tier to `gpt-?.?-terra*` and up one effort
#      notch, CLAMPED at xhigh (no `max`): bare -> high, low -> medium,
#      medium -> high, high -> xhigh, xhigh -> xhigh. (`ui-coding` is the one
#      exception -- see item 8.) See also item 6: the `gpt-[0-9].[0-9]`
#      migration-fallback twin is gone entirely.
#   2. Gemini globs re-anchored `gemini-[3-9]*-...`. The old
#      `gemini-*-flash-preview` resolved to `gemini-omni-flash-preview` (the
#      Omni lane, not the Flash lane) AND missed gemini-3.5/3.6/3.7/3.8-flash
#      entirely, because Google dropped the `-preview` suffix. Verified with
#      the shipped `_version_sort_key`.
#   3. Gemini thinking is now requested through the knob the provider actually
#      reads -- `extra_request_params.thinking_config.thinking_level`. A
#      `reasoning_effort` on a gemini candidate is INERT at every value (see
#      matrix_loader.INERT_CONFIG_RULES) and is rejected by the loader.
#      Gemini's ceiling is `high`; there is no xhigh/max. `minimal` is NOT
#      used -- gemini-3.8-flash errors on it.
#   4. Image generation retargeted `nano-banana-pro-preview` ->
#      `gemini-3-pro-image`. The old name is absent from list_models() and
#      from every first-party Google source; `gemini-3-pro-image` is the GA
#      flagship image model ("Nano Banana Pro"). No thinking_level: Gemini 3
#      image models think by default and expose no level knob.
#   5. GitHub Copilot pins refreshed to the newest models the live API lists
#      (claude-sonnet-5, claude-opus-5, gpt-5.5). This also fixes an invalid
#      effort: claude-sonnet-4.6 and claude-opus-4.6 accept only
#      low/medium/high/max -- an `xhigh` on either was silently wrong. The
#      repo test passes a provider-wide superset, so it did not catch this.
#   6. The `gpt-[0-9].[0-9]` twin was DROPPED from every openai candidate.
#      It existed as a pre-5.6 migration fallback; `gpt-5.6-terra` and
#      `gpt-5.6-luna` are both confirmed present on the live openai
#      list_models(), so a fallback to gpt-5.5-or-earlier buys nothing. The
#      Copilot GPT slots moved the same generation for the same reason
#      (`gpt-?.?-terra*` + `gpt-5.6-terra`, both live on the Copilot API).
#   7. NO CANDIDATE IS LEFT WITH A BLANK EFFORT. An unset knob is not a
#      neutral default to reason about, so every candidate that can carry one
#      now says what it wants; `medium` where nothing more specific was
#      established. Three exceptions, all enforced by the loader rather than
#      chosen here (matrix_loader.INERT_CONFIG_RULES):
#        * claude-haiku-* on ANY provider -- `reasoning_effort` is REJECTED at
#          every value (measured: effort absent on the wire, budget pinned at
#          32000). Its documented replacement is `thinking_budget_tokens`.
#        * gemini/* -- `reasoning_effort` is REJECTED; `thinking_level` is the
#          live knob and is set on every gemini candidate except flash-lite,
#          where Google documents no levels for the text models.
#        * ollama `model: "*"` -- no effort knob exists.
#   8. `ui-coding` is `gpt-?.?-luna*` at `max` effort on EVERY matrix that
#      routes to openai -- cheap tier, maximum thinking.
#   9. OpenAI is now TERRA-OR-LUNA ONLY. The `gpt-?.?-mini*` and
#      `gpt-?.?-nano*` rungs were removed alongside the `gpt-[0-9].[0-9]`
#      twin (item 6): with sol paused, every openai candidate in every matrix
#      is one of exactly two globs -- `gpt-?.?-terra*` (work) or
#      `gpt-?.?-luna*` (cheap/fast, and `ui-coding` at max effort). There are
#      no openai fallback rungs left at all.
#  10. Haiku carries `thinking_budget_tokens: 32000` instead of a blank. The
#      loader REJECTS `reasoning_effort` on any model whose name contains
#      'haiku', under any provider -- it collapses every level above 'low'
#      into one identical request. 32000 is what `medium` resolves to and what
#      a blank already produced on the wire, so this is explicit, not a
#      behaviour change. REVISIT WHEN HAIKU 5 SHIPS: if it gains real effort
#      support, swap back to `reasoning_effort` -- and note the guard rule
#      itself must be updated in the same change, since it matches the
#      substring 'haiku' and would otherwise keep rejecting claude-haiku-5.
#  12. The separate `openai-chatgpt.yaml` matrix was FOLDED INTO openai.yaml
#      and deleted. The ChatGPT backend is the same models on a different
#      bill, not a different tier story, so it did not warrant a second file
#      to keep in sync -- every role in openai.yaml now carries a candidate
#      for each backend.
#  13. Every openai-family candidate glob lost its trailing `*`
#      (`gpt-?.?-terra`, `gpt-?.?-luna`). The ChatGPT backend serves a `-fast`
#      sibling for every model, and a trailing `*` silently selects it (tie on
#      version, longer name wins). Suffix-free, ONE glob vocabulary resolves
#      identically on both backends. The preset `tier_ladder` deliberately
#      keeps its `*`: it classifies an already-chosen model rather than
#      selecting one, so it must still match a hand-pinned suffixed id.

name: openai
description: "OpenAI routing for BOTH backends -- the pay-per-use API (`openai`) and the ChatGPT subscription (`openai-chatgpt`). Flagship (sol) tier paused pending evals; knob-consistent delegation ON by default -- sub-agents inherit the caller's tier/effort ceiling."
updated: "2026-09-07"

# ---------------------------------------------------------------------------
# Knob-consistent delegation preset -- DEFAULT ON for this matrix.
# ---------------------------------------------------------------------------
preset:
  # Documentation only; never read by code.
  axis: [cheap, fast]

  # Cheapest -> most expensive, per provider family. Declared, never inferred
  # from price: no per-token price for the 5.6 family exists in any guide, so
  # this encodes the vendor's own ordinal statements (luna lowest cost, terra
  # mid, sol flagship). A rung may be one glob or a list of globs.
  #
  # `gpt-[0-9].[0-9]` sits on the SOL rung on purpose: since the 5.6 rename the
  # bare alias points at the flagship tier.
  #
  # THE LADDER KEEPS ITS TRAILING `*` even though the candidates above dropped
  # theirs. These two globs do opposite jobs: a candidate glob SELECTS one
  # model (narrow is right -- it is how `-fast` is excluded), while a ladder
  # glob CLASSIFIES a model that is already chosen (broad is right -- a user
  # who pins `gpt-5.6-terra-fast` by hand must still land on the terra rung).
  # Narrowing these would silently un-classify every suffixed id.
  #
  # One ladder per BACKEND, because a ladder is keyed by the matrix
  # `provider:` string verbatim (knob_consistency.py:420) -- there is no
  # family aliasing. The rungs are identical: same models, different bill.
  tier_ladder:
    openai:
      - ["gpt-?.?-luna*", "gpt-?.?-mini*", "gpt-?.?-nano*"]
      - ["gpt-?.?-terra*"]
      - ["gpt-?.?-sol*", "gpt-[0-9].[0-9]"]
    openai-chatgpt:
      - ["gpt-?.?-luna*", "gpt-?.?-mini*", "gpt-?.?-nano*"]
      - ["gpt-?.?-terra*"]
      - ["gpt-?.?-sol*", "gpt-[0-9].[0-9]"]

  delegation:
    # none | effort | tier-and-effort | strict
    #
    # `strict` matches the measured arm exactly (l1-knob-consistent-routing
    # shipped and measured `strict`, not `tier-and-effort` + escalation) --
    # this default follows the evidence, not a stronger untested claim.
    inherit: strict
    report_unhonored: true

  # KNOWN LIMITATION, on the ChatGPT backend only.
  #
  # TIER inheritance works on both backends: the ladders above are what the
  # clamp reads to pick a lower-tier candidate, and they are equivalent.
  #
  # EFFORT inheritance does NOT reach `openai-chatgpt`. The clamp writes the
  # inherited effort to `preset.effort_key_for(family)`
  # (knob_consistency.py:638-644), which resolves to the canonical
  # `reasoning_effort` for every family -- and that key is exactly the one
  # this provider does not read. So a chatgpt candidate keeps the effort its
  # `extra_request_params` block declares, rather than the caller's.
  #
  # This is stated rather than worked around because the two available
  # workarounds are both worse: `effort_unsupported` matches on MODEL globs,
  # and both backends serve the same model ids, so silencing chatgpt would
  # silence the API backend too. The real fix is to let `effort_keys` name a
  # nested path (e.g. `extra_request_params.reasoning.effort`), which is a
  # change to knob_consistency, not to this file.

roles:
  general:
    description: "Versatile catch-all, no specialization needed"
    candidates:
      - provider: openai
        model: gpt-?.?-terra
        config:
          reasoning_effort: high
        # Same models, same glob as the `openai` candidate above -- this is
        # the ChatGPT-subscription backend rather than the pay-per-use API,
        # and it is a separate provider MODULE, so it needs its own
        # candidate: `provider: openai` does not match it. Effort rides
        # `extra_request_params` because this provider reads no effort key
        # from mount config (matrix_loader.INERT_CONFIG_RULES).
      - provider: openai-chatgpt
        model: gpt-?.?-terra
        config:
          extra_request_params:
            reasoning:
              effort: high
              summary: detailed

  fast:
    description: "Quick utility tasks — parsing, classification, file ops, bulk work"
    candidates:
      - provider: openai
        model: gpt-?.?-luna
        config:
          reasoning_effort: medium
        # Same models, same glob as the `openai` candidate above -- this is
        # the ChatGPT-subscription backend rather than the pay-per-use API,
        # and it is a separate provider MODULE, so it needs its own
        # candidate: `provider: openai` does not match it. Effort rides
        # `extra_request_params` because this provider reads no effort key
        # from mount config (matrix_loader.INERT_CONFIG_RULES).
      - provider: openai-chatgpt
        model: gpt-?.?-luna
        config:
          extra_request_params:
            reasoning:
              effort: medium
              summary: detailed

  coding:
    description: "Code generation, implementation, debugging"
    candidates:
      - provider: openai
        model: gpt-?.?-terra
        config:
          reasoning_effort: high
        # Same models, same glob as the `openai` candidate above -- this is
        # the ChatGPT-subscription backend rather than the pay-per-use API,
        # and it is a separate provider MODULE, so it needs its own
        # candidate: `provider: openai` does not match it. Effort rides
        # `extra_request_params` because this provider reads no effort key
        # from mount config (matrix_loader.INERT_CONFIG_RULES).
      - provider: openai-chatgpt
        model: gpt-?.?-terra
        config:
          extra_request_params:
            reasoning:
              effort: high
              summary: detailed

  ui-coding:
    description: "Frontend/UI code — components, layouts, styling, spatial reasoning"
    candidates:
      - provider: openai
        model: gpt-?.?-luna
        config:
          reasoning_effort: max
        # Same models, same glob as the `openai` candidate above -- this is
        # the ChatGPT-subscription backend rather than the pay-per-use API,
        # and it is a separate provider MODULE, so it needs its own
        # candidate: `provider: openai` does not match it. Effort rides
        # `extra_request_params` because this provider reads no effort key
        # from mount config (matrix_loader.INERT_CONFIG_RULES).
      - provider: openai-chatgpt
        model: gpt-?.?-luna
        config:
          extra_request_params:
            reasoning:
              effort: max
              summary: detailed

  security-audit:
    description: "Vulnerability assessment, attack surface analysis, code auditing"
    candidates:
      - provider: openai
        model: gpt-?.?-terra
        config:
          reasoning_effort: xhigh
        # Same models, same glob as the `openai` candidate above -- this is
        # the ChatGPT-subscription backend rather than the pay-per-use API,
        # and it is a separate provider MODULE, so it needs its own
        # candidate: `provider: openai` does not match it. Effort rides
        # `extra_request_params` because this provider reads no effort key
        # from mount config (matrix_loader.INERT_CONFIG_RULES).
      - provider: openai-chatgpt
        model: gpt-?.?-terra
        config:
          extra_request_params:
            reasoning:
              effort: xhigh
              summary: detailed

  reasoning:
    description: "Deep architectural reasoning, system design, complex multi-step analysis"
    candidates:
      - provider: openai
        model: gpt-?.?-terra
        config:
          reasoning_effort: xhigh
        # Same models, same glob as the `openai` candidate above -- this is
        # the ChatGPT-subscription backend rather than the pay-per-use API,
        # and it is a separate provider MODULE, so it needs its own
        # candidate: `provider: openai` does not match it. Effort rides
        # `extra_request_params` because this provider reads no effort key
        # from mount config (matrix_loader.INERT_CONFIG_RULES).
      - provider: openai-chatgpt
        model: gpt-?.?-terra
        config:
          extra_request_params:
            reasoning:
              effort: xhigh
              summary: detailed

  critique:
    description: "Analytical evaluation — finding flaws in existing work, not generating solutions"
    candidates:
      - provider: openai
        model: gpt-?.?-terra
        config:
          reasoning_effort: xhigh
        # Same models, same glob as the `openai` candidate above -- this is
        # the ChatGPT-subscription backend rather than the pay-per-use API,
        # and it is a separate provider MODULE, so it needs its own
        # candidate: `provider: openai` does not match it. Effort rides
        # `extra_request_params` because this provider reads no effort key
        # from mount config (matrix_loader.INERT_CONFIG_RULES).
      - provider: openai-chatgpt
        model: gpt-?.?-terra
        config:
          extra_request_params:
            reasoning:
              effort: xhigh
              summary: detailed

  creative:
    description: "Design direction, aesthetic judgment, high-quality creative output"
    candidates:
      - provider: openai
        model: gpt-?.?-terra
        config:
          reasoning_effort: high
        # Same models, same glob as the `openai` candidate above -- this is
        # the ChatGPT-subscription backend rather than the pay-per-use API,
        # and it is a separate provider MODULE, so it needs its own
        # candidate: `provider: openai` does not match it. Effort rides
        # `extra_request_params` because this provider reads no effort key
        # from mount config (matrix_loader.INERT_CONFIG_RULES).
      - provider: openai-chatgpt
        model: gpt-?.?-terra
        config:
          extra_request_params:
            reasoning:
              effort: high
              summary: detailed

  writing:
    description: "Long-form content — documentation, marketing, case studies, storytelling"
    candidates:
      - provider: openai
        model: gpt-?.?-terra
        config:
          reasoning_effort: high
        # Same models, same glob as the `openai` candidate above -- this is
        # the ChatGPT-subscription backend rather than the pay-per-use API,
        # and it is a separate provider MODULE, so it needs its own
        # candidate: `provider: openai` does not match it. Effort rides
        # `extra_request_params` because this provider reads no effort key
        # from mount config (matrix_loader.INERT_CONFIG_RULES).
      - provider: openai-chatgpt
        model: gpt-?.?-terra
        config:
          extra_request_params:
            reasoning:
              effort: high
              summary: detailed

  research:
    description: "Deep investigation, information synthesis across multiple sources"
    candidates:
      - provider: openai
        model: gpt-?.?-terra
        config:
          reasoning_effort: xhigh
        # Same models, same glob as the `openai` candidate above -- this is
        # the ChatGPT-subscription backend rather than the pay-per-use API,
        # and it is a separate provider MODULE, so it needs its own
        # candidate: `provider: openai` does not match it. Effort rides
        # `extra_request_params` because this provider reads no effort key
        # from mount config (matrix_loader.INERT_CONFIG_RULES).
      - provider: openai-chatgpt
        model: gpt-?.?-terra
        config:
          extra_request_params:
            reasoning:
              effort: xhigh
              summary: detailed

  vision:
    description: "Understanding visual input — screenshots, diagrams, UI mockups"
    candidates:
      - provider: openai
        model: gpt-?.?-terra
        config:
          reasoning_effort: medium
        # Same models, same glob as the `openai` candidate above -- this is
        # the ChatGPT-subscription backend rather than the pay-per-use API,
        # and it is a separate provider MODULE, so it needs its own
        # candidate: `provider: openai` does not match it. Effort rides
        # `extra_request_params` because this provider reads no effort key
        # from mount config (matrix_loader.INERT_CONFIG_RULES).
      - provider: openai-chatgpt
        model: gpt-?.?-terra
        config:
          extra_request_params:
            reasoning:
              effort: medium
              summary: detailed

  image-gen:
    description: "Image generation (limited — gpt-image not available via chat API)"
    candidates:
      - provider: openai
        model: gpt-?.?-terra
        config:
          reasoning_effort: high
        # Same models, same glob as the `openai` candidate above -- this is
        # the ChatGPT-subscription backend rather than the pay-per-use API,
        # and it is a separate provider MODULE, so it needs its own
        # candidate: `provider: openai` does not match it. Effort rides
        # `extra_request_params` because this provider reads no effort key
        # from mount config (matrix_loader.INERT_CONFIG_RULES).
      - provider: openai-chatgpt
        model: gpt-?.?-terra
        config:
          extra_request_params:
            reasoning:
              effort: high
              summary: detailed

  critical-ops:
    description: "High-reliability operational tasks — infrastructure, orchestration, coordination"
    candidates:
      - provider: openai
        model: gpt-?.?-terra
        config:
          reasoning_effort: xhigh
        # Same models, same glob as the `openai` candidate above -- this is
        # the ChatGPT-subscription backend rather than the pay-per-use API,
        # and it is a separate provider MODULE, so it needs its own
        # candidate: `provider: openai` does not match it. Effort rides
        # `extra_request_params` because this provider reads no effort key
        # from mount config (matrix_loader.INERT_CONFIG_RULES).
      - provider: openai-chatgpt
        model: gpt-?.?-terra
        config:
          extra_request_params:
            reasoning:
              effort: xhigh
              summary: detailed
